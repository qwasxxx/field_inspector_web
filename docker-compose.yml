services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fi
      POSTGRES_PASSWORD: fi
      POSTGRES_DB: field_inspector
    ports:
      - "5432:5432"
    volumes:
      - fi_pg_data:/var/lib/postgresql/data

volumes:
  fi_pg_data:
